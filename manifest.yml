declared-services:
  TodoList-MongoDB:
    label: compose-for-mongodb
    plan: Standard
applications:
- name: TodoListMongoDBApp
  memory: 256M
  instances: 2
  random-route: true
  buildpack: swift_buildpack
  services:
  - TodoList-MongoDB
